{% extends "base.html.twig" %}

{% block body %}

    <h1>Add a new Category</h1>

    {{ form_start(form) }}

        {{ form_row(form.label) }}

    <button type="submit">Add</button>

    {{ form_end(form) }}

 {% endblock body %}